/*
 * Filter bar's stylesheet for '5-index'
 */

/* class Filter's properties */
.filters {
	background-color: #FFFFFF;
	height: 70px;
	width: 100%;
	border: 1px solid #DDDDDD;
	border-radius: 4px;
}

/* Filters' child class 'button' properties */
.filters button {
	font-size: 18px;
	background-color: #FF5A5F;
	color: #FFFFFF;
	height: 48px;
	width: 20%;
	border: none;
	border-radius: 4px;
	float: right;
	margin: 12px 30px;
}

/* Hover subclasses property */
.filters button:hover {
	opacity: .9;
}

/* Filters' child class 'div' properties */
.filters div {
	display: inline-block; 
	height: 100%;
	width: 25%;
	justify-items: center;
	align-items: center;
	text-align: center;	
}

/* class location's properties */
.locations {
	border-right: 1px solid #DDDDDD;
}

/* Subclass definition for h3 */
.filters h3 {
	margin-bottom: 0;
	font-weight: 600px;
}

/* Subclass definition for h4 */
.filters h4 {
	margin-top: 0;
	font-weight: 400px;
	font-size: 14px;
}
